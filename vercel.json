{
  "version": 2,
  "name": "ai-portfolio",
  "framework": "create-react-app",
  "buildCommand": "npm run build && cd admin && npm install && npm run build && cd .. && node scripts/combine-builds.js",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "env": {
    "CI": "false",
    "REACT_APP_API_URL": "/api",
    "REACT_APP_USE_FALLBACK": "false"
  }
}
